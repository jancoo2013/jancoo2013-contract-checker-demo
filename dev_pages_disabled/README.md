# Disabled developer pages

Files in this directory are kept for developer reference only.

They are intentionally outside Streamlit's auto-discovered `pages/` directory so they do not appear in the normal MVP sidebar during user testing.

To run one of these pages manually, move it back into `pages/` on a temporary local branch or copy the relevant helper logic into a local diagnostic script.
